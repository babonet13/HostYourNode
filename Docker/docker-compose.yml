version: '3'
services:
    ubuntu-pkg:
        build:
            context: ./ubuntu-pkg
            dockerfile: Dockerfile
    ubuntu-bin:
        build:
            context: ./ubuntu-bin
            dockerfile: Dockerfile
    ubuntu-src:
        build:
            context: ./ubuntu-src
            dockerfile: Dockerfile
