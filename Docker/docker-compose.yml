version: '3'
services:
    ubuntu-pkg:
        build:
            context: ./ubuntu-pkg
            dockerfile: ./ubuntu-pkg/Dockerfile
    ubuntu-bin:
        build:
            context: ubuntu-bin
            dockerfile: ubuntu-bin/Dockerfile
    ubuntu-src:
        build:
            context: ubuntu-src
            dockerfile: ubuntu-src/Dockerfile
